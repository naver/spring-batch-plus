# Simple Reader Adapter Samples

## Invariants

- Samples should cover a single delegate whose reader produces one item at a
  time.
- Every supported combination of reader, processor, and writer roles should have
  a runnable sample.
- Lifecycle callback support should have a runnable sample.
