# Delete Metadata Job Samples

## Invariants

- Deleting with the default configuration should have a runnable sample.
- Running as a dry run should have a runnable sample.
- Every `DeleteMetadataJobBuilder` option should have a runnable sample.
