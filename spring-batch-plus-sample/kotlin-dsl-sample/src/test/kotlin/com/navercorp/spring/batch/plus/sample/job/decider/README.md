# Job Decider Samples

## Invariants

- Job flow guide examples should cover deciders supplied by bean lookup and by
  variable, including their required transitions.
- Transition DSL examples are covered in the sibling
  [`transition`](../transition) package.
