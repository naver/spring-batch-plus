# Job Step Selection Samples

## Invariants

- The [`sequential`](sequential) package should cover step selections by bean
  lookup, inline creation, and variable input on the simple-job path.
- The [`transition`](transition) package should cover the same input forms on
  the flow-job path.
- General transition DSL examples are covered in the sibling
  [`transition`](../transition) package.
