# Job Flow Selection Samples

## Invariants

- The [`sequential`](sequential) package should cover flow selections by bean
  lookup, inline creation, and variable input without explicit transitions.
- The [`transition`](transition) package should cover the same input forms with
  explicit transitions.
- General transition DSL examples are covered in the sibling
  [`transition`](../transition) package.
